# ===================================
#  List Column Definitions
# ===================================

columns:
    photoThumb:
        label: Photo
        type: partial
        sortable: no
    sort_order:
        label: Order
        invisible: true
